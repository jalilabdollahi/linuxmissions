# Speak Variable

## What you practiced
Positional parameters let shell scripts receive input from the command line. `$1` is the first argument, `$2` is the second, and so on.

## Commands to remember
```bash
bash greet.sh World
echo "$1"
echo "Hello, $1!"
```
