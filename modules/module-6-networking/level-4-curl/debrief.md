# API Call

## What you practiced
`curl` is useful even with local files because the same toolchain works for remote APIs later. Once the response is flowing through the shell, standard text processing can take over.

## Commands to remember
```bash
curl -s file:///tmp/example.json
curl -o output.json URL
grep -o '"status": "[^"]*"'
```
