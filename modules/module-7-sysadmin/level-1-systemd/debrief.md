# Service Down

## What you practiced
`systemctl enable` controls whether a service starts automatically, while `systemctl start` changes its active runtime state right now.

## Commands to remember
```bash
systemctl enable myapp.service
systemctl start myapp.service
systemctl status myapp.service
```
