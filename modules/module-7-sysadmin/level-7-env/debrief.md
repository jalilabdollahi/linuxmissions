# Env Surgery

## What you practiced
Environment files are simple, but many startup failures boil down to missing variables or variables that were never exported into the process environment.

## Commands to remember
```bash
source .env
set -a
source .env
set +a
printenv APP_MODE
```
